# Instalado en /etc/systemd/journald.conf.d/ por scripts/install.sh.
# Logs persistentes (sobreviven reinicios) con tope de espacio para no
# llenar la SD en un equipo que puede pasar meses sin mantenimiento.
[Journal]
Storage=persistent
SystemMaxUse=200M
SystemKeepFree=500M
MaxRetentionSec=3month
